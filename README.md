boxboss-phpunit-php-timer
=========================

PHP Unit Dependency - Timer. Here because its easier. Also this version if fixed to use static instead of instantiated for fields and methods. Normal way didnt work